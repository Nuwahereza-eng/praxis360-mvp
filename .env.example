# Local SQLite for demo (Prisma schema stays PostgreSQL-compatible)
DATABASE_URL="file:./dev.db"

# JWT session signing secret (change in production)
AUTH_SECRET="praxis360-demo-secret-change-me"

# AI provider (optional). If unset, deterministic fallback is used.
AI_PROVIDER=""
AI_API_KEY=""
